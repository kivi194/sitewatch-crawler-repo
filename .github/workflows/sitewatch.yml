name: SiteWatch Crawler
on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:
    inputs:
      site:
        description: "Domain to scan (e.g. israelpharm.com). Leave empty to scan all sites."
        required: false
        default: ""
      test_mode:
        description: "Run mode: 'priority' for fast transaction tests only, leave empty for full crawl."
        required: false
        default: ""
jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements.txt
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Install Playwright browsers
        run: playwright install chromium --with-deps
      - name: Run crawler
        env:
          CRAWLER_INGEST_SECRET: ${{ secrets.CRAWLER_INGEST_SECRET }}
          SITE_FILTER: ${{ inputs.site }}
          TEST_MODE: ${{ inputs.test_mode }}
        run: python checker.py
